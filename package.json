{
  "name": "flutter-coder-mcp",
  "version": "2.1.0",
  "description": "Flutter/Dart MCP server: live API doc lookups, pub.dev package data, curated widget/error catalogs, Dart language reference, code generation, and a verified advanced-sample + knowledge corpus for building Flutter apps.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "check-updates": "node scripts/check-updates.mjs",
    "pretest": "npm run build",
    "test": "node --test test/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.15.17",
    "typescript": "^5.9.3"
  }
}
